export { loadResume, saveResume, exportResume, importResume, exportAsStandardJSONResume } from './resume';

// 向后兼容的别名
export { loadResume as loadConfig, saveResume as saveConfig, exportResume as exportConfig, importResume as importConfig } from './resume';
